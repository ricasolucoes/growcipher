---
description: Git Branching Rules
---

# Git Branching Rules

- ALWAYS use `master` as the default branch name.
- NEVER create or use a branch named `main`.
- When initializing a new repository or migrating, ensure the primary branch is set to `master`.
- Run `git config --global init.defaultBranch master` to enforce this behavior if not already set.
